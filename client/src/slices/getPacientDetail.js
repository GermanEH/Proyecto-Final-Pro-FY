import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const getPacientDetail = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = getPacientDetail.actions

export default getPacientDetail.reducer