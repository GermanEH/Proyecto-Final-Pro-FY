import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const putProfessional = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = putProfessional.actions

export default putProfessional.reducer