import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const deleteProfessional = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = deleteProfessional.actions

export default deleteProfessional.reducer