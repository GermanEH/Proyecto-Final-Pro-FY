import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const getSpecialties = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = getSpecialties.actions

export default getSpecialties.reducer