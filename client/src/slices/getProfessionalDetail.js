import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const getProfessionalDetail = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = getProfessionalDetail.actions

export default getProfessionalDetail.reducer