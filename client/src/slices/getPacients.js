import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const getPacients = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = getPacients.actions

export default getPacients.reducer