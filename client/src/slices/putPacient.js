import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const putPacient = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = putPacient.actions

export default putPacient.reducer