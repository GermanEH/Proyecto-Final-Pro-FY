import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const deletePacient = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = deletePacient.actions

export default deletePacient.reducer